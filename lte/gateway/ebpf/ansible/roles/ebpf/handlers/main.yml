---
- name: reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: restart ebpf datapath
  ansible.builtin.systemd:
    name: "{{ item }}"
    state: restarted
  loop:
    - magma-ebpf-gtp.service
    - magma-ebpf-adapter.service
